#include <iostream>
#include <format>
#include <cstdlib>
#include <time.h>
using namespace std;
int main() {
	int array[2][2];
	srand(static_cast<unsigned int>(time(NULL)));
	for (int i = 0; i < 2; i++) {
		for (int j = 0; j < 2; j++) {
			array[i][j];

		}
	}

	//for (auto arr : array) {
	//	cout << format("{} ", arr);
	//}



	
}